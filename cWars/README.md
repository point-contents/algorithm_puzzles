Running the bash file "makenew.sh" will create a new folder with the given argument
as the folder name, create an entry in the cmakelist for the whole directory, and 
make a new boilerplate cpp file in the created directory

