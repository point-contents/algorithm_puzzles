#include <algorithm>
#include <assert.h>
#include <cmath>
#include <iostream>
#include <map>
#include <numeric>
#include <string>
#include <vector>

// Found a python version here
// but I dont quite understand it yet
// https://gist.github.com/phillipkent/5270266
// Will try to figure it out another time

#define LOG(x) std::cout << x << "\n";

template <typename T>
void printVec(T vec)
{
    for (auto& things : vec) {
        std::cout << things << "\n";
    }
}

void iterate(int a, int b)
{
    std::vector < if (a % b == 0)
    {
        std::gcd(a, b);
    }

    class Decomp {
    public:
        static std::string decompose(const std::string& nrStr, const std::string& drStr);
    };

    int main(int argc, char* argv[])
    {

        return 0;
    }
